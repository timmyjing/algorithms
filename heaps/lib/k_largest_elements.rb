require_relative 'heap'
# Let's use our BinaryMinHeap to solve a whiteboarding-style question. 
# Given an `array` and an integer `k`, return the k-largest elements 
# in `O(nlogk)` time.

def k_largest_elements(array, k)
end
